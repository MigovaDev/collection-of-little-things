import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    paddingVertical: 16,
    paddingHorizontal: 24,
    borderRadius: 8,
    borderWidth: 1,
  },
  buttonGhost: {
    paddingVertical: 0,
    paddingHorizontal: 0,
  },
  buttonText: {
    fontSize: 16,
    fontWeight: '600',
  },
  buttonTextGhost: {
    fontSize: 14,
    fontWeight: '400',
  },
});
