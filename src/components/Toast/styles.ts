import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  toast: {
    position: 'absolute',
    bottom: 40,
    left: 24,
    right: 24,
    alignItems: 'center',
  },
  toastText: {
    fontSize: 14,
    fontWeight: '500',
  },
});

