import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  themeOption: {
    alignItems: 'center',
    gap: 8,
  },
  themePreview: {
    borderRadius: 16,
    borderWidth: 3,
    alignItems: 'center',
    justifyContent: 'center',
  },
  themeName: {
    fontSize: 18,
    fontWeight: '600',
  },
  selectedLabel: {
    fontSize: 14,
    fontWeight: '500',
    height: 20,
  },
});
